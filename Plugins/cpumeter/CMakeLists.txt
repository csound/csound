make_plugin(cpumeter cpumeter.c)
if(CLOCK_GETTIME)
    target_compile_definitions(cpumeter PRIVATE "HAVE_CLOCK_GETTIME")
endif()
if (LINUX)
    target_compile_definitions(cpumeter PRIVATE "LINUX")
endif()