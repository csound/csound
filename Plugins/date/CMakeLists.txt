make_plugin(date date.c)
if(BARE_METAL)
    target_compile_definitions(date PRIVATE "BARE_METAL")
endif()
if (LINUX)
    target_compile_definitions(date PRIVATE "LINUX")
endif()
if (WIN32)
    target_compile_definitions(date PRIVATE "WIN32")
endif()