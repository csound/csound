<!-- Generated by scripts/opcode_deprecations.py; edit H/opcode_deprecations.def. -->
# Deprecated opcode catalog

209 legacy names and renamed aliases. See [the maintenance policy](opcode-deprecation.md)
before changing historical behavior. Replacements may need different arguments or data.

| Opcode | Replacement | Policy | Notes |
| --- | --- | --- | --- |
| `ATSadd` | `atsadd` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATSaddnz` | `atsaddnz` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATSbufread` | `atsbufread` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATScross` | `atscross` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATSinfo` | `atsinfo` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATSinterpread` | `atsinterpread` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATSpartialtap` | `atspartialtap` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATSread` | `atsread` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATSreadnz` | `atsreadnz` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ATSsinnoi` | `atssinnoi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `K35_hpf` | `k35hpf` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `K35_lpf` | `k35lpf` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `MixerClear` | `mixerclear` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `MixerGetLevel` | `mixergetlevel` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `MixerReceive` | `mixerreceive` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `MixerSend` | `mixersend` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `MixerSetLevel` | `mixersetlevel` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `MixerSetLevel_i` | `mixersetleveli` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `OSCbundle` | `oscbundle` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `OSCcount` | `osccount` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `OSCinit` | `oscinit` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `OSCinitM` | `oscinitm` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `OSClisten` | `osclisten` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `OSCraw` | `oscraw` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `OSCsend` | `oscsend` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `OSCsend_lo` | `oscsendlo` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `arduinoRead` | `arduinoread` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `arduinoReadF` | `arduinoreadf` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `arduinoStart` | `arduinostart` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `arduinoStop` | `arduinostop` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `array` | `fillarray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `bformdec` | `bformdec1` | FROZEN | Known inaccurate legacy decoder; use the supported decoder for new code. |
| `bformenc` | `bformenc1` | FROZEN | Known inaccurate legacy encoder; bformenc1 omits the old gain arguments. |
| `chn_S` | `chnS` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `chn_a` | `chna` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `chn_array` | `chnarray` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `chn_k` | `chnk` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `chnclear_array` | `chncleararray` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `cntCreate` | `cntcreate` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `cntCycles` | `cntcycles` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `cntDelete` | `cntdelete` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `cntDelete_i` | `cntdeletei` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `cntRead` | `cntread` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `cntReset` | `cntreset` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `cntState` | `cntstate` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `copy2ftab` | `copya2ftab` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `copy2ttab` | `copyf2array` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `count_i` | `counti` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `diode_ladder` | `diodeladder` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `event_i` | `eventi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `fin` | `diskin2` | LEGACY | Adapt the file-reading arguments and output assignment; not a drop-in rename. |
| `genarray_i` | `genarrayi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `hrtfer` | `hrtfstat` | FROZEN | Legacy HRTFcompact behavior is frozen; hrtfstat uses different data and arguments. See [PR #3009](https://github.com/csound/csound/pull/3009). |
| `inq` | `in` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `ins` | `in` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `ktableseg` | `tableseg` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `lentab` | `lenarray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `loop_ge` | `loopge` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `loop_gt` | `loopgt` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `loop_le` | `loople` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `loop_lt` | `looplt` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `maparray_i` | `maparrayi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `max_k` | `maxk` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `maxtab` | `maxarray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `mintab` | `minarray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `mp3in` | `diskin2` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `mp3len` | `filelen` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `nchnls_hw` | `nchnlshw` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `oscbnk` | `oscilbank` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `out32` | `out` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outh` | `out` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outo` | `out` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outq` | `out` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outq1` | `outch` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outq2` | `outch` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outq3` | `outch` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outq4` | `outch` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outs` | `out` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outs1` | `outch` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outs2` | `outch` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `outx` | `out` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `pop` | No direct replacement documented | LEGACY | Retained for compatibility; no direct replacement is documented. |
| `pop_f` | No direct replacement documented | LEGACY | Retained for compatibility; no direct replacement is documented. |
| `print_type` | `printtype` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `printf_i` | `printfi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `ptable` | `table` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `ptable3` | `table3` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `ptablei` | `tablei` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `ptableiw` | `tablew` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `ptablew` | `tablew` | LEGACY | Use tablew with the matching signal rate; i-rate writing has different timing. |
| `push` | No direct replacement documented | LEGACY | Retained for compatibility; no direct replacement is documented. |
| `push_f` | No direct replacement documented | LEGACY | Retained for compatibility; no direct replacement is documented. |
| `sc_lag` | `lag` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `sc_lagud` | `lagud` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `sc_phasor` | `trigphasor` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `sc_trig` | `trighold` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `scalet` | `scalearray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `sclag` | `lag` | ALIAS | Deprecated alias documented in the manual; use the supported name. |
| `sclagud` | `lagud` | ALIAS | Deprecated alias documented in the manual; use the supported name. |
| `scoreline_i` | `scorelinei` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `scphasor` | `trigphasor` | ALIAS | Deprecated alias documented in the manual; use the supported name. |
| `sctrig` | `trighold` | ALIAS | Deprecated alias documented in the manual; use the supported name. |
| `serialBegin` | `serialbegin` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `serialEnd` | `serialend` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `serialFlush` | `serialflush` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `serialPrint` | `serialprint` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `serialRead` | `serialread` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `serialWrite` | `serialwrite` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `serialWrite_i` | `serialwritei` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `sliderKawai` | `sliderkawai` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `sndload` | No direct replacement documented | LEGACY | The manual says the loaded data cannot be used; remove the call and load audio through a supported reader. |
| `soundout` | `fout` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `soundouts` | `fout` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `specaddm` | No direct replacement documented | FROZEN | Legacy weighted addition is frozen, including the unused multiplier. See [PR #3007](https://github.com/csound/csound/pull/3007). |
| `specdiff` | No direct replacement documented | LEGACY | Part of the legacy constant-Q w-signal chain; no direct replacement is documented. |
| `specdisp` | No direct replacement documented | LEGACY | Part of the legacy constant-Q w-signal chain; no direct replacement is documented. |
| `specfilt` | No direct replacement documented | LEGACY | Part of the legacy constant-Q w-signal chain; no direct replacement is documented. |
| `spechist` | No direct replacement documented | LEGACY | Part of the legacy constant-Q w-signal chain; no direct replacement is documented. |
| `specptrk` | No direct replacement documented | LEGACY | Part of the legacy constant-Q w-signal chain; no direct replacement is documented. |
| `specscal` | No direct replacement documented | LEGACY | Part of the legacy constant-Q w-signal chain; no direct replacement is documented. |
| `specsum` | No direct replacement documented | LEGACY | Part of the legacy constant-Q w-signal chain; no direct replacement is documented. |
| `spectrum` | No direct replacement documented | FROZEN | Legacy constant-Q w-signal analysis is frozen. PVS analysis is not a drop-in replacement. See [PR #3008](https://github.com/csound/csound/pull/3008). |
| `stack` | No direct replacement documented | LEGACY | Retained for compatibility; no direct replacement is documented. |
| `sumtab` | `sumarray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `system_i` | `systemi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `tab_i` | `tabi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `tabgen` | `genarray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `tableiw` | `tablew` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `tabmap` | `maparray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `tabmap_i` | `maparrayi` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `tabmapi` | `maparrayi` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `tabslice` | `slicearray` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `tabw_i` | `tabwi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `tb0` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb0_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb0init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb1` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb10` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb10_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb10init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb11` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb11_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb11init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb12` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb12_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb12init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb13` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb13_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb13init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb14` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb14_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb14init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb15` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb15_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb15init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb1_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb1init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb2` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb2_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb2init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb3` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb3_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb3init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb4` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb4_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb4init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb5` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb5_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb5init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb6` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb6_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb6init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb7` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb7_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb7init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb8` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb8_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb8init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb9` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb9_init` | `tab` | ALIAS | Use tab expressions directly; the old table-slot setup is not needed. |
| `tb9init` | `tab` | LEGACY | Use tab expressions directly; the old table-slot setup is not needed. |
| `trim_i` | `trimi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `turnoff2_i` | `turnoff2i` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vadd_i` | `vaddi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vaddv_i` | `vaddvi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vbap16` | `vbap` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `vbap4` | `vbap` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `vbap4move` | `vbapmove` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `vbap8` | `vbap` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `vbap8move` | `vbapmove` | LEGACY | Retained for compatibility; put new behavior in the supported replacement. |
| `vcopy_i` | `vcopyi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vdivv_i` | `vdivvi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vexp_i` | `vexpi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vexpv_i` | `vexpvi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vmult_i` | `vmulti` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vmultv_i` | `vmultvi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vpow_i` | `vpowi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vpowv_i` | `vpowvi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `vsubv_i` | `vsubvi` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `xscanmap` | `scanmap` | LEGACY | Review matrix layout and scan setup when migrating; not a drop-in rename. |
| `xscans` | `scans` | LEGACY | Review matrix layout and scan setup when migrating; not a drop-in rename. |
| `xscansmap` | `scansmap` | LEGACY | Review matrix layout and scan setup when migrating; not a drop-in rename. |
| `xscanu` | `scanu` | LEGACY | Review matrix layout and scan setup when migrating; not a drop-in rename. |
| `xyin` | No direct replacement documented | LEGACY | No direct replacement is documented; choose a host or GUI input method for the application. |
| `zdf_1pole` | `zdf1pole` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `zdf_1pole_mode` | `zdf1polemode` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `zdf_2pole` | `zdf2pole` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `zdf_2pole_mode` | `zdf2polemode` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
| `zdf_ladder` | `zdfladder` | ALIAS | Renamed alias; maintain the shared implementation through its supported name. |
